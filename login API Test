-name: "Maseno App – Login API"
    type: api
    subtype: http
    request:
      url: "https://api.masenotrading.com/login"
      method: POST
      headers:
        Content-Type: application/json
      body: |
        {
          "email": "test@masenotrading.com",
          "password": "TestPassword123"
        }
    assertions:
      - type: statusCode
        operator: is
        target: 200
      - type: jsonPath
        operator: isNotEmpty
        target: "$.token"
    options:
      tick_every: 600
    tags:
      - app:maseno-trading
      - env:production
      - type:auth
